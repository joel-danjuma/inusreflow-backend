# Deploy-time secrets -- created directly on the VPS, never present or
# meant to be committed from any machine. See backend/.gitignore and
# frontend/.gitignore for the per-app equivalents.
.env
.insureflow_app_password

.DS_Store
