import * as React from 'react';

/**
 * CopyableToken — from frontend@0.1.0.
 */
export interface CopyableTokenProps {
  [key: string]: unknown;
}

export declare const CopyableToken: React.ComponentType<CopyableTokenProps>;
