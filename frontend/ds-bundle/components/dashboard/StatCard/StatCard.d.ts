import * as React from 'react';

/**
 * StatCard — from frontend@0.1.0.
 */
export interface StatCardProps {
  [key: string]: unknown;
}

export declare const StatCard: React.ComponentType<StatCardProps>;
